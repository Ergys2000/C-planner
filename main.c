#include <stdio.h>
#include "planner.h"
int main(){
	// 0 -> Means that nothing is specified
	// 1 -> Means that time is specified only get work
	// n -> Means that everything is specified, just create the schedule
	Planner(1);
	return 0;
}
